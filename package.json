{
  "name": "test",
  "version": "1.0.0",
  "main": "app.js",
  "scripts": {
    "build": "npx tsc",
    "prestart": "npm run build",
    "start": "node dist/app.js",
    "lint": "eslint . --ext .ts"
  }
}
